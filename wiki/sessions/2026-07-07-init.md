---
type: session
title: "2026-07-07 — Initial build"
status: done
created: 2026-07-07
updated: 2026-07-07
tags: [session]
---

# Session: Initial build (2026-07-07)

## Goal
Initialize the project from an empty directory: a MERN study app encapsulating the Frontend
Interview Handbook, with live-editable code blocks + reset, backed by memory in this vault.

## Done
- Monorepo scaffold (npm workspaces), root `CLAUDE.md`, `.gitignore`, README, git init.
- [[client]]: Vite/React/Tailwind, design tokens (light/dark), AppShell/Sidebar/TopBar, 4 pages.
- [[showcase-framework]]: `LiveCode` (Sandpack + key-remount reset), `ChallengeView`, `QuizCard`.
- [[curriculum]]: 4 exemplars across all four categories (challenge model at init; later restructured into Tracks → Modules → Lessons).
- [[server]]: Express + Mongoose (Progress, Note) with graceful no-DB fallback.
- [[decisions]]: recorded 5 ADRs (Sandpack, reset, Atlas, offline-first, content-as-objects).
- This memory vault scaffolded (Mode B + sessions).

## Verified
- `npm install` → 380 packages, 0 vulnerabilities.
- `npm run build --workspace client` → 267 modules, no errors.
- Server boots; `/api/progress` GET/POST respond; no-DB path returns `persisted:false`.

## Handoffs (owner)
- Create `server/.env` with Atlas `MONGO_URI` to enable persistence.
- Install referenced skills via `/plugin marketplace add` (Karpathy, ui-ux-pro-max, frontend-design).

## Next session ideas
- Add more challenges (tabs, modal, throttle, curry, flexbox gallery, more quizzes).
- Build the per-challenge notes UI (server API already exists).
- Load the design skills and do a visual polish pass.
